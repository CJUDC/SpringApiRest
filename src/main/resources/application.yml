spring:
  application:
    name: spring-boot-cero-a-experto
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  cache:
    type: simple

springdoc:
  swagger-ui:
    path: /swagger-ui.html